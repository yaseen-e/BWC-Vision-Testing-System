try:
        while True:
            match current_state:

except KeyboardInterrupt:
        # This catches CTRL+C from the terminal so you can exit gracefully
        print("\n[ALERT] Manual override triggered. Forcing Shutdown.")
        current_state = SystemState.SHUTDOWN
        # servo_driver.disable_all()
        print("System safely powered down.")