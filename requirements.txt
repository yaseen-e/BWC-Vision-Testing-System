# BWC-Vision-Testing-System Python dependencies (pip)

# Image processing
opencv-python
Pillow
rapidocr_onnxruntime
numpy

# Utilities
pyserial

# Servo / hardware (Adafruit PCA9685 + Blinka)
adafruit-circuitpython-servokit
adafruit-circuitpython-pca9685
adafruit-blinka
lgpio

# Notes:
# The following system packages should be installed via apt on Raspberry Pi OS:
# - Picamera2 / libcamera Python bindings: sudo apt install python3-picamera2 python3-libcamera libcamera-apps
#
# Create your virtual environment with system site-packages so Picamera2 is accessible:
# python3 -m venv .venv --system-site-packages